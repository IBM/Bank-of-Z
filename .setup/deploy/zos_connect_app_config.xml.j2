<server>
    <webApplication id="{{ api_name | lower }}" location="${server.config.dir}/apps/{{ api_name | lower }}.war" name="{{ api_name  | lower }}" contextRoot="/{{ api_name  | lower  }}"/>
</server>
